apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: react-todo
  namespace: default
  annotations:
    kubernetes.io/ingress.class: "traefik"
spec:
  rules:
    - host: mmalabugin.ru
      http:
        paths:
          - path: /react-todo
            pathType: Prefix
            backend:
              service:
                name: react-todo
                port:
                  number: 80
  tls:
    - hosts:
        - mmalabugin.ru
      secretName: main-page-tls
